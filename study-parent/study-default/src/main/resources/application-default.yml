server:
  port: ${my.server.port:80}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${my.mysql.url:jdbc:mysql://91beiming.com:3306/demo?useSSL=false&allowPublicKeyRetrieval=true}
    username: root
    password: goudan125
  redis:
    host: 91beiming.com
    port: 6379
    password: goudan125
    database: 0
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl